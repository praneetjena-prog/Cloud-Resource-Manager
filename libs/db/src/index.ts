export { PrismaClient } from "@prisma/client";
export type {
  User,
  CreditTransaction,
  CloudResource,
  SupportTicket,
} from "@prisma/client";
export {
  ResourceType,
  ResourceStatus,
  TransactionType,
  TicketStatus,
  TicketPriority,
} from "@prisma/client";
